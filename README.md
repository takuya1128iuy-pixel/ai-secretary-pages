# AI秘書の認証ページ

Google認証審査に使用している公開リポジトリ。GitHub Pagesで次の2ページを公開している。

- `index.html`: アプリの説明
- `privacy.html`: プライバシーポリシー

## 現在の扱い

- すでに公開とGoogle側の登録は完了している。
- ファイル名とURLを変えない。
- `main` への送信は公開そのものなので、内容を確認して承認を得てから行う。
- 公開リポジトリのため、個人情報、顧客情報、鍵、内部資料を追加しない。

`tetsuzuki-compass/` と `hoken-navi/` は古い写し。本番のてつづきコンパスはCloudflare Pagesにあり、このリポジトリでは更新しない。

開発時の入口は `AGENTS.md`、現在の状態は `docs/status.md`。
