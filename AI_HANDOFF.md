# AI_HANDOFF.md — 引き継ぎ資料（ai-secretary-pages）

Claude Code / Codex など、複数の AI エージェントでこのリポジトリを触るための資料。
最終更新: **2026-09-08**

**このリポジトリは小さく（HTML 6 枚）、ほぼ止まっている。** だがひとつだけ、
知らずに触ると困るものが残っているので、そこを中心に書く。

- **Codex に依頼するときは、まずこのファイルを読ませること。**
- **このファイルに API キー・トークン・個人情報・認証情報を書かない。**

---

## ⚠️ 最初に読むこと

### 1. このリポジトリは public。GitHub Pages でそのまま世に出ている

`main` に push した内容は、誰でも見られる URL に出る。
**個人が特定できるものを足さない。** 名前・住所・電話・家族のこと・顧客のこと。
連絡先はこのアプリ専用に作ったアドレスを使っている（README 参照）。差し替えない。

### 2. 本来の役目と、あとから相乗りしたものが混ざっている

| 中身 | 役目 | 状態 |
|---|---|---|
| `index.html` / `privacy.html` | **本来の役目。** AI秘書の Google OAuth 審査に出すホームページとプライバシーポリシー | **現役。動かさない** |
| `tetsuzuki-compass/` | てつづきコンパス（保険の手続き案内）の**古い写し** | **要判断**（下） |
| `hoken-navi/` | 「移転しました」の案内ページ | 置いてあるだけ |

### 3. `tetsuzuki-compass/` は 1 世代古いものが公開されたまま

**商品の本番は Cloudflare Pages（private リポジトリ `~/tetsuzuki-compass`）に移っている。**
ここに残っているのは移転前の写しで、いまも public な URL で開ける。

| 場所 | 収録社数 | 状態 |
|---|---|---|
| `~/tetsuzuki-compass`（private・Cloudflare Pages） | **37社** | **本番** |
| ここ `tetsuzuki-compass/`（public・GitHub Pages） | 36社 | **古い写しが公開されたまま** |

さらに、`~/tetsuzuki-compass-src/line-richmenu` の `SITE_BASE_URL` の**既定値がこちらを向いたまま**。
**既定のままリッチメニューを deploy すると、古いページに飛ぶ。**

**どうするかは利用者の判断**（残す・更新する・「移転しました」に差し替える）。
**勝手に消さない。勝手に上書きしない。** 消すと、古い URL を渡した相手のリンクが切れる。

---

## プロダクト概要

**ai-secretary-pages** — AI秘書（`~/claudecode-Secretary`）の **Google OAuth の公開ステータスを
「テスト中」→「本番」に切り替えるため**に作った、ホームページとプライバシーポリシーの 2 枚。

これがあることで「7 日ごとに Google の許可を取り直す」問題が解消した（2026-09-05）。
**つまり、この 2 枚は消すと AI秘書の Google 連携が壊れる。**

---

## 現在のアーキテクチャと主要ディレクトリ

```
.
├── README.md                     公開の手順と、OAuth 審査まわりの注意
├── AI_HANDOFF.md                 このファイル
├── index.html                    アプリのホームページ（OAuth 審査で出す）
├── privacy.html                  プライバシーポリシー（OAuth 審査で出す）
├── hoken-navi/index.html         「移転しました」の案内
└── tetsuzuki-compass/            てつづきコンパスの古い写し
    ├── index.html                お客さま向け
    ├── logo-og.png
    └── pro/index.html            募集人向け
```

ビルドも依存もフレームワークも無い。素の HTML。

---

## 開発環境・起動方法・テスト方法

```bash
open index.html      # そのままブラウザで開くだけ
```

テストは無い。CI も無い。`main` に push すると GitHub Pages が自動で更新する
（Settings → Pages で「Deploy from a branch」`main` / `root`）。

環境変数・鍵・トークンは**ひとつも使っていない。**

---

## 外部サービスと連携状況

| 相手 | 状態 | メモ |
|---|---|---|
| **GitHub Pages（public）** | 稼働中 | `main` から自動。`takuya1128iuy-pixel/ai-secretary-pages` |
| **Google Cloud（OAuth 同意画面）** | 連動 | `index.html` と `privacy.html` の URL を、同意画面のホームページ／プライバシーポリシーに登録済み。**本番公開済み** |
| **AI秘書（`~/claudecode-Secretary`）** | 依存される側 | この 2 枚が消えると Google 連携の前提が崩れる |
| **てつづきコンパス** | 古い写しが同居 | 本番は Cloudflare（`~/tetsuzuki-compass`） |

### Google OAuth まわりで残っている論点

`gmail.readonly` は Google の分類で **「制限付き（restricted）」**。本番公開後に審査や
セキュリティ評価を求められることがある。求められたら、そこで判断する。
どうしても通らないときは AI秘書側の `SCOPES` から `gmail.readonly` を外せば、
残りは「機微（sensitive）」だけになる（`trip_scan_mail` は使えなくなる）。

---

## 重要な設計判断

1. **審査用のページを、アプリ本体のリポジトリに入れなかった。**
   本体（`claudecode-Secretary`）は private で家族のことが書いてある。
   審査に出すページは public でなければならないので、入れ物を分けた。
2. **ここには個人が特定できるものを置かない。** 連絡先はこのアプリ専用に作ったアドレス。
3. **素の HTML のまま置く。** ビルドを挟むと、審査で出す URL が壊れたときに切り分けが増える。

---

## 現在進行中の作業

2026-09-08 時点。**作業ツリーはクリーン**、`main` と `origin/main` は同じ（`24f8bc1`）。

**進行中の作業は無い。** 役目を果たして止まっている状態。
最後に動いたのは 2026-09-06〜07 で、てつづきコンパスのロゴ差し替えの反映まで。

---

## 次に取り組む候補

1. **`tetsuzuki-compass/` の古い写しをどうするか決める**（上の「⚠️ 3」）。いちばん大きい未決事項。
   決めたら `~/tetsuzuki-compass-src/line-richmenu` の `SITE_BASE_URL` の既定も揃える。
2. **`hoken-navi/` を残すか決める。** 移転案内なので、いつかは畳める。
3. Google から審査を求められたときの対応（上の「残っている論点」）。

**基本は「触らない」でよいリポジトリ。** 手を入れる用事ができたときだけ開く。

---

## 注意点・既知の問題

- **public。** 個人が特定できるものを足さない。
- **`index.html` と `privacy.html` の URL を変えない。** Google の同意画面に登録済みで、
  変えると OAuth 側の設定も直すことになる。ファイル名・パスをそのまま保つこと。
- **`tetsuzuki-compass/` を勝手に消さない・勝手に更新しない。** 上の判断待ち。
- **README の公開手順は「これから公開する人」向けに書かれている。** すでに公開済みなので、
  手順どおりに `git init` からやり直さないこと。

---

## 共同作業の決めごと

1. **手を入れる前に `git status` と `git log --oneline -5` を見る。**
   他の作業者の差分が残っていたら、混ぜずに先に聞く。
2. **同じ作業ツリーを 2 体で同時に編集しない。** 並行するなら別ブランチか git worktree。
3. **`main` への push（＝公開）は、必ず利用者に確認してから。**
4. **消さない。** 要らなそうなものは、そう言うだけにして手は出さない。
5. **大きな設計判断・実装の完了・未解決の問題・引き継ぎが出たら、このファイルを更新する。**
6. **このファイルに鍵・トークン・個人情報を書かない。**

---

## 更新履歴

| 日付 | 内容 |
|---|---|
| 2026-09-08 | 初版。`24f8bc1` 時点で作成 |
