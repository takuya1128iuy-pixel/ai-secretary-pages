# このリポジトリを触るとき

**まず [AI_HANDOFF.md](AI_HANDOFF.md) を読むこと。** Codex 向けの方針は [AGENTS.md](AGENTS.md) にあり、
中身は同じ。**片方を直したら、もう片方も直す。**

## いちばん大事なこと — ここは public

`main` に置いたものは、そのまま GitHub Pages で世に出る。

1. **個人が特定できるものを書かない。** 名前・住所・電話・家族のこと・顧客のこと。
   `~/claudecode-Secretary/CLAUDE.md` のような中身を、ここへ写さない。
2. **`git push` はしない。** ここへの push は「世に出す」ことそのもの。
   必要だと思ったら、たくやさんに言うだけにする。
3. **`index.html` と `privacy.html` の URL を変えない。** Google の同意画面に登録済み。
4. **README の公開手順どおりに `git init` からやり直さない。** すでに公開済み。
5. **消さない。** 要らなそうなものは、そう言うだけにして手は出さない。

## 何が入っているか

| | 役割 |
| --- | --- |
| `index.html` / `privacy.html` | AI秘書の Google 認証審査に出したページ2枚。**このリポジトリの本来の役目** |

**これだけ。** 以前ここに相乗りしていた てつづきコンパスの古い写し（`tetsuzuki-compass/`）と、
その旧名の転送ページ（`hoken-navi/`）は、2026-09-09 に消した。**あの URL はいま 404。**
てつづきコンパスの本番は Cloudflare（https://tc-6fs139.pages.dev/ ）で、
直すならソース側のリポジトリ（非公開）。

役目を果たして止まっているリポジトリなので、**基本は「触らない」でよい。**

## 区切りがついたら

`git commit` する（メッセージは日本語で、**なぜそう直したか**を書く）。
**push はたくやさんが判断する。**
