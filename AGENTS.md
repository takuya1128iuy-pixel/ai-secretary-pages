# 公開済み認証ページの開発ガイド

Google認証審査用ページをGitHub Pagesで公開しているリポジトリ。通常は変更しない。共通の役割分担と作業上の約束は `docs/shared/rules.md` を読む。

## 変えてはいけない境界

- このリポジトリは公開されている。個人情報、顧客情報、鍵、内部資料を置かない。
- `main` への送信は公開操作。明示依頼なしに送信しない。
- `index.html` と `privacy.html` のURLを変えない。Googleの登録先になっている。
- `tetsuzuki-compass/` と `hoken-navi/` は 2026-09-09 に削除済み。**あの URL はいま 404。**
  戻さない。てつづきコンパスの本番は Cloudflare で、直すならソース側のリポジトリ。
- READMEの過去の公開手順を再実行しない。すでに公開済み。

## 共通ルール

- 話し方と報告のしかた: `docs/shared/rules.md`

**この箱は public なので、共通ルールのうちこの1枚だけを置く。**
役割分担・過去の学び・公開の引き金（`roles.md` `lessons.md` `publishing.md`）には
private リポジトリの名前が並ぶため、ここには置かない。
正本は別の非公開リポジトリにある。**このリポジトリのコピーは直さない。**

現在の役割は `docs/status.md` を参照する。`AI_HANDOFF.md` は過去の詳しい経緯を調べるときだけ読む。
